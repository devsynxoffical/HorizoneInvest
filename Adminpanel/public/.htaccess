RewriteEngine On
RewriteBase /admin/

# Serve existing files/directories directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Admin SPA fallback for direct /admin/* links
RewriteRule ^ index.html [L]
